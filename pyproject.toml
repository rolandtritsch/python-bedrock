[project]
name = "python-bedrock"
version = "0.1.0"
description = "My Python AWS Bedrock Playground"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.38.36",
]

[tool.setuptools.packages.find]
include = ["*"]
exclude = ["infrastructure*"]
